services:
  - type: web
    name: crackcode
    runtime: docker
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
      - key: DATABASE_URL
        sync: false  # Set to your Neon Postgres URL
      - key: CLAUDE_MODEL
        value: claude-sonnet-4-20250514
